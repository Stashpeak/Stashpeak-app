{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only capabilities. The self-updater plugin is not built for mobile (see Cargo.toml / lib.rs cfg(desktop) gating), so its permission must not be required on iOS/Android.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": ["updater:default"]
}
