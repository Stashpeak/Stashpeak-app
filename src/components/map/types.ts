export interface MapNodeTone {
  surfaceFill: string;
  badgeFill: string;
  badgeBorder: string;
  badgeText: string;
  metricFill: string;
  metricBorder: string;
  minimapColor: string;
  edgeColor: string;
}

export type MapLinkState = "linked" | "unlinked" | "standalone";
